
each game in games
  h5
    a(href='/pitcher/' + pitcher.id + '/game/' + game.id) #{pitcher.playercity} #{pitcher.playerteam} vs. #{game.opponentcity} #{game.opponentname} (#{game.date.getMonth()+1}/#{game.date.getDate()}/#{game.date.getFullYear()})
    hr
  
